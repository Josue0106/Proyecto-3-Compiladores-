let
  const n ~ 5;
  var total : Integer;
  var idx : Integer;
  func cuadrado(i : Integer) : Integer ~ i * i
in
  begin
    total := 0;
    idx := 0;
    while idx < n do
      begin
        total := total + cuadrado(idx);
        idx := idx + 1
      end;
    putint(total);
    putnewline
  end
